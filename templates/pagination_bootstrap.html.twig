{% if pageCount > 1 %}
    <nav aria-label="Page navigation">
        <ul class="pagination">
            {% if previousPage is defined %}
                <li class="page-item">
                    <a class="page-link" href="{{ path(app.request.attributes.get('_route'), query|merge({(app.request.query.get('page') - 1)}) ) }}" aria-label="Previous">
                        <span aria-hidden="true">&laquo;</span>
                    </a>
                </li>
            {% else %}
                <li class="page-item disabled">
                    <span class="page-link">&laquo;</span>
                </li>
            {% endif %}

            {% for page in pagesInRange %}
                <li class="page-item {{ page == current ? 'active' : '' }}">
                    <a class="page-link" href="{{ path(app.request.attributes.get('_route'), query|merge({page}) ) }}">{{ page }}</a>
                </li>
            {% endfor %}

            {% if nextPage is defined %}
                <li class="page-item">
                    <a class="page-link" href="{{ path(app.request.attributes.get('_route'), query|merge({(app.request.query.get('page') + 1)}) ) }}" aria-label="Next">
                        <span aria-hidden="true">&raquo;</span>
                    </a>
                </li>
            {% else %}
                <li class="page-item disabled">
                    <span class="page-link">&raquo;</span>
                </li>
            {% endif %}
        </ul>
    </nav>
{% endif %}
